✅ สรุปที่ใช้บ่อยที่สุด
การกระทำ	คำสั่ง
# เปิดทั้งหมด	
docker compose up -d
# ปิดทั้งหมด	
docker compose down
# รีสตาร์ต CTFd	
docker compose restart ctfd
# ดู log CTFd	
docker compose logs -f ctfd
# ตรวจ service ที่รันอยู่ 
docker compose ps


docker compose down -v
docker compose up -d --build

docker compose build --no-cache orchestrator
docker compose up -d orchestrator

docker compose logs --tail=200 orchestrator
docker compose logs --tail=200 ctfd

docker compose ps
docker stats

source .venv/bin/activate

docker build -t challenge-red_ghost_login ./challenges/red/ghost_login
docker build -t challenge-red_pivot_notes ./challenges/red/pivot_notes
docker build -t challenge-red_protocol_probe ./challenges/red/protocol_probe
docker build -t challenge-red_log_poisoning ./challenges/red/log_poisoning

docker exec -it analytics_db psql -U analytics -d analytics


export CTFD_TOKEN="ctfd_bca6416572d9bf9aedd98587ea2641e8f12fef62a0a24adaa1da56da885426bc"

gemini_api="REDACTED_GEMINI_KEY"

#restart tailscale
sudo systemctl restart tailscaled
sudo tailscale up
tailscale status
ip addr show tailscale0



